# Logit Fixtures

M3 should add small reference logits or next-token outputs here. Do not commit
large model weights. Each fixture must name the source tool, model revision,
prompt, tolerance, and regeneration command.
