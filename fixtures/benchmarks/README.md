# Benchmark Fixtures

These fixtures describe benchmark harness shape only. They are small committed
JSON examples used by default tests to validate schema, command, timing, output,
and skip-record fields without running local model artifacts or whisper.cpp.

Real benchmark outputs belong under `local-artifacts/benchmarks/` and must not
be committed.
